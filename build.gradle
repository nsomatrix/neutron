
subprojects {
    apply plugin: 'java'
    apply plugin: 'maven-publish'

    group = 'org.neutron'
    version = '1.0'

    repositories {
        mavenCentral()
    }

    // Common dependencies or configurations can go here
}
